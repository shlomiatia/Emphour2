* Never put any comments in the code
* Always create scenes and set their attributes in tscn file (unless it's truly created dynamically)
* Use c style syntax - && || ! instead of and or not
* Do not create nodes or classes that are designed for other objects to inheirt - prefer composition
* If function A use function B, function A should appear before function B in the file
* Keep code short - files under 100 lines or so, functions under 10 lines or so
* Avoid using "Node" node to hold a code referencing other nodes in a different node, unless it's state
* Avoid using deferred


This project is a new version of the game C:\Users\shlom\OneDrive\Documents\GitHub\CardConquest

It will take some code and resources from it, but not all